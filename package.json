{
  "name": "filenamemaker",
  "version": "1.0.0",
  "description": "Standardized filename builder and batch file renamer",
  "main": "js/app.js",
  "scripts": {
    "build": "mkdir -p dist && cp index.html sw.js robots.txt dist/ && cp -r assets dist/ && esbuild js/app.js --bundle --minify --outfile=dist/js/app.js && esbuild css/style.css --minify --outfile=dist/css/style.css",
    "dev": "wrangler pages dev ."
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "wrangler": "^3.0.0"
  }
}
